======================================================================
10/04/04
======================================================================

(1) Added in the function

	  bool CShellPoint::IsWhitePreHard()

the new line:

          if( FieldBlock.wHalfCount[3] > 0) return true; // 10/04/04

and in the function

          bool CShellPoint::IsBlackPreHard()

the new line:

          if( FieldBlock.bHalfCount[3] > 0) return true; // 10/04/04

(2)  Default level set as '1' instead of '3'


=======================================================================
11/04/04
=======================================================================

Added in the function
     
	 UINT CPattern::CalcWeight( CMoveData* pid, int ListNum, eDirect direct, ePower power)

the lines:

         weight = ( weight + 5) * 5/2; // 11/04/04 // 11/04/04

		 insted of:  //weight = ( weight + 5) *3 / 2;

and:

        weight = ( weight + 5) * 5/2; // 11/04/04; // 11/04/04

		 insted of:  //weight = ( weight + 5) *3 / 2;
